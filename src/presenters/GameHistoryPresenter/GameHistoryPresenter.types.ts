import GetGamesFromStore from '../../interactors/GetGamesFromStore';

interface iGameHistoryPresenterProps {
	navigation: any;
	getGamesFromStore: GetGamesFromStore;
}

export default iGameHistoryPresenterProps;
